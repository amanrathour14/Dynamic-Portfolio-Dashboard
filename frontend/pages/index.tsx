// The main page that imports and uses the dashboard component.

import React from 'react';
import App from './components/App';

const HomePage = () => {
  return <App />;
};

export default HomePage;